#!/usr/bin/env bash

function test_is_within() (
	source "$DOROTHY/sources/bash.bash"

	my_array=(2 1)
	__print_lines "${my_array[@]}"
	__has --source={my_array} 1
)


# fire if invoked standalone
if [[ "$0" = "${BASH_SOURCE[0]}" ]]; then
	test_is_within "$@"
fi
